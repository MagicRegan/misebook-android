<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MiseBook" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/brand_bg_dark</item>
        <item name="android:statusBarColor">@color/brand_bg_dark</item>
        <item name="android:navigationBarColor">@color/brand_bg_dark</item>
    </style>
    <style name="Theme.MiseBook.Splash" parent="Theme.MiseBook" />
</resources>
